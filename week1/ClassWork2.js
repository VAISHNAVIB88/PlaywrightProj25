const browserVersion='Chrome'

function getBrowserVersion(){
    if (browserVersion==="Chrome") {
        const browserVersion = "Chrome Version is 125.0"
        console.log("Version is : " +browserVersion)
    } 
        console.log("Browser is :" +browserVersion)
}
getBrowserVersion();
